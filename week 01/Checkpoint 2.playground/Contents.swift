import UIKit

var library: [String] = ["History Book", "History Book", "Cook Book", "Math Book", "Science Book", "Writing Book", "Cook Book"]
var librarySet = Set(library)

//print number of items in array
print("There are \(library.count) items and \(librarySet.count) unique items")
